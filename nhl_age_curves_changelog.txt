=========================================================
NHL AGE CURVES - CHANGELOG
=========================================================

v0.57.2 - readme.txt engine sync
---------------------------------------------------------
- CHANGED: Updated `readme.txt` so its projection pipeline description matches
  current runtime behavior in `nhl/player_pipeline.py` and `nhl/constants.py`.
- CHANGED: Documented that `NO_PROJECTION_METRICS = {'GP', 'SH%', 'TOI'}`
  suppresses forecast lines entirely, so those metrics do not enter KNN or the
  linear fallback in normal app flow.
- CHANGED: Corrected the Age-mode projection split to show the duplicated last
  real point in the projection trace (`proj_part` starts at `Age >= max_age`).
- CHANGED: Synced the `readme.txt` import graph and constants export summary to
  the current `dialog.py`, `chart.py`, `player_pipeline.py`, and `constants.py`.
- VALIDATED: Re-read the patched `readme.txt` sections against current source
  files to confirm the handoff doc now matches the active engine behavior.

v0.57.3 - AI readiness safety rails
---------------------------------------------------------
- ADDED: Deterministic unittest coverage for URL state encoding, historical
  goalie rate sanitation, goalie inference, KNN clamp behavior, and goalie GP
  fallback durability rules.
- ADDED: GitHub Actions workflow to install requirements and run
  `python -m unittest discover -s tests -v` on push, pull request, and manual dispatch.
- ADDED: `AI_CHANGE_CHECKLIST.md` with a concise high-risk change checklist for
  AI and human contributors touching projections, baselines, data loaders, or docs.
- CHANGED: Updated `README.md` so the repository advertises the test suite,
  CI workflow, and contributor safety rails.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 12/12 tests.

v0.57.4 - internalize AI workflow files
---------------------------------------------------------
- CHANGED: Removed the public `README.md` references to AI testing, CI, and the
  internal checklist so the public readme stays product-focused.
- CHANGED: Added an Internal Workflow section to `CLAUDE.md` requiring post-
  implementation test runs and test updates when behavior changes.
- CHANGED: Updated `.gitignore` to keep `AI_CHANGE_CHECKLIST.md`, `.github/`,
  and `tests/` local-only alongside the existing ignored internal docs.

v0.57.5 - footer version sync rule
---------------------------------------------------------
- CHANGED: Added a `CLAUDE.md` rule requiring the visible footer version in
  `app.py` to be updated in tandem with each new latest changelog entry.
- CHANGED: Synced the `app.py` footer version string to `v0.57.5` so it now
  matches the newest changelog version.
- CHANGED: Added an inline reminder comment above the `app.py` footer so editors
  see the changelog-to-footer sync rule at the exact version string location.

v0.57.6 - cleaner control labels
---------------------------------------------------------
- CHANGED: Simplified the top control labels from `Select Metric` to `Metric`
  and from `X-Axis` to `X axis` for a cleaner toolbar.
- CHANGED: Renamed the view toggles from `Forecast`, `Era-Adjust`, and
  `Show Prime` to `Projection`, `Era adjust`, and `Prime years`.
- CHANGED: Updated the goalie era-adjust helper caption and related inline docs
  so the control copy stays consistent.

v0.57.7 - mobile header compaction
---------------------------------------------------------
- CHANGED: Added a dedicated `.page-header` style so the logo and `Age Curves`
  title stay on a single line.
- CHANGED: Reduced mobile top padding and shrank the mobile header title size
  to free more above-the-fold space on phones.
- CHANGED: Shrunk the NHL logo on mobile so the header reads cleaner on narrow
  screens without changing the desktop layout.

v0.57.8 - custom favicon branding
---------------------------------------------------------
- ADDED: A custom `assets/favicon.svg` badge using the app's silver, blue, and
  red palette with a hockey puck plus rising curve motif for tiny-size clarity.
- CHANGED: Wired `app.py` page config to use the custom favicon via the new
  `nhl.styles.get_favicon_path()` helper so the browser tab shows branded chrome.
- ADDED: Focused unittest coverage to verify the favicon asset exists and that
  `app.py` stays connected to the favicon helper.

v0.57.9 - clearer headline and share title
---------------------------------------------------------
- CHANGED: Updated the browser/share title to `NHL Analytics | Age Curves,`
  `Projections & Player Comparisons` so search and shared links explain the app
  more clearly than the old short title.
- CHANGED: Replaced the on-page header text with `NHL Analytics` and added a
  concise tagline covering age curves, career projections, player comparisons,
  and team trends.
- ADDED: Focused unittest coverage to keep the new title and headline copy from
  drifting during future UI edits.

v0.58.0 - compact on-demand share links
---------------------------------------------------------
- CHANGED: Share URLs now use player IDs and team abbreviations only, so copied
  links are much shorter than the old name-heavy query strings.
- CHANGED: Default state values are now omitted from share links, trimming the
  query string down to only the settings that actually differ from the app's
  defaults.
- CHANGED: The browser URL now stays clean while exploring and the compact
  share URL is only generated when the chart's `Copy link` control is clicked.
- ADDED: Backward-compatible decoding for older long-form shared URLs plus
  focused tests covering compact encoding, default omission, and on-demand
  share-link UI wiring.
- FIXED: Removed the temporary page-level share button and status message so
  the existing chart clipboard control handles on-demand link sharing by itself.

v0.58.1 - team dropdown input alignment
---------------------------------------------------------
- FIXED: Normalized the Team Comparison dropdown control so its text sizing,
  left padding, and vertical alignment match the Skater and Goalie Global
  Search field more closely.
- CHANGED: Scoped the Team sidebar selectbox styling behind a dedicated anchor
  so the normalization only affects that first Team selector.
- VALIDATED: `python -m unittest tests.test_styles -v` passed locally with 5/5
  tests after adding a regression check for the Team dropdown style hook.

v0.58.2 - team label normalization correction
---------------------------------------------------------
- FIXED: Replaced the bold `st.subheader("Team Comparison")` heading with the
  Team selectbox's native visible label so it matches the Skater and Goalie
  `Global Search` label size, weight, and placement above the field.
- VALIDATED: `python -m unittest tests.test_styles -v` passed locally after
  tightening the sidebar regression test around the Team selector label.

v0.58.3 - team top spacing normalization
---------------------------------------------------------
- FIXED: Removed the extra empty Team anchor block that was leaving too much
  space between the top divider and the `Team Comparison` label.
- CHANGED: Targeted the first selectbox after the sidebar divider directly so
  the Team label and field spacing now follow the same compact pattern as
  `Global Search` without needing a separate spacer element.
- VALIDATED: `python -m unittest tests.test_styles -v` passed locally after
  updating the style regression to assert the post-divider selectbox rule.

v0.58.4 - team baseline toggle disable
---------------------------------------------------------
- FIXED: Greyed out the `Baseline` toggle in Team mode so it now reads as a
  disabled control instead of an available comparison option.
- FIXED: Team mode no longer forwards baseline overlays to the chart, even if
  `do_base` was left enabled in player mode before switching categories.
- VALIDATED: `python -m unittest tests.test_styles -v` passed locally after
  adding a regression check for the disabled Team baseline control.

v0.58.5 - tighter top header spacing
---------------------------------------------------------
- FIXED: Collapsed the extra whitespace between the hero text and the top
  `Category & Metric` panel by consolidating the subtitle and divider into a
  single compact header block.
- CHANGED: Replaced the long subtitle with `Hockey like never before!` and
  moved the divider directly underneath it.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.5`.
- VALIDATED: `python -m unittest tests.test_styles -v` and
  `python -m unittest discover -s tests -v` both passed locally.

v0.58.6 - age analytics rebrand pass
---------------------------------------------------------
- CHANGED: Renamed the on-page hero title from `NHL Analytics` to
  `Age Analytics`.
- CHANGED: Shortened the browser tab title to `NHL Age Analytics`.
- CHANGED: Replaced the repository description with
  `Hockey analytics like never before!`.
- FIXED: Removed the thin white divider below the hero copy.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.6`.
- VALIDATED: `python -m unittest tests.test_styles -v` and
  `python -m unittest discover -s tests -v` both passed locally.

v0.58.7 - required category selection guard
---------------------------------------------------------
- FIXED: The sidebar category segmented control can no longer be deselected into
  an empty state, so a second click on the active segment no longer mixes Skater,
  Goalie, and Team logic.
- FIXED: Added an app-level fallback guard in `app.py` so any invalid
  `stat_category` value is coerced back to `Skater` before controls and
  pipelines run.
- ADDED: Focused regression coverage in `tests/test_sidebar.py` for category
  sanitization, deselect handling, and the app-level invalid-state guard.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.7`.
- VALIDATED: `python -m unittest tests.test_sidebar -v` and
  `python -m unittest discover -s tests -v` both passed locally.

v0.58.8 - header guide modal
---------------------------------------------------------
- ADDED: A compact circular `?` button beside the hero header that opens a
  modal explaining the app's basics from the top of the page.
- ADDED: A concise `How This App Works` dialog covering what the app shows,
  how projection works, what is ML-ish versus rule-based, and how skater and
  goalie baselines differ without exposing the full recipe.
- CHANGED: Made the new header guide button sticky near the top of the page so
  it stays reachable while scrolling.
- CHANGED: Added targeted header-guide regression coverage in
  `tests/test_styles.py` so the new button and dialog stay wired in.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.8`.
- VALIDATED: `python -m unittest tests.test_styles -v` and
  `python -m unittest discover -s tests -v` both passed locally.

v0.58.9 - header guide alignment pass
---------------------------------------------------------
- FIXED: Moved the header guide button into alignment with the main title row
  instead of the full hero block, so it now sits level with the header.
- CHANGED: Split the subtitle onto its own row and tightened the title/button
  spacing so the header stays compact.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.9`.
- VALIDATED: `python -m unittest tests.test_styles -v` and
  `python -m unittest discover -s tests -v` both passed locally.

v0.59.0 - sidebar guide relocation
---------------------------------------------------------
- FIXED: Moved the app guide trigger out of the page header and into the sidebar
  above the category buttons, which is a much cleaner home for it.
- CHANGED: Restored the hero header to its simple title-plus-subtitle layout now
  that the guide trigger no longer lives in the top-right corner.
- CHANGED: Updated the guide regression check so it now verifies the sidebar
  button wiring instead of the old header placement.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.10`.
- VALIDATED: `python -m unittest tests.test_styles -v`,
  `python -m unittest tests.test_sidebar -v`, and
  `python -m unittest discover -s tests -v` all passed locally.

v0.60.0 - sidebar guide button fix
---------------------------------------------------------
- FIXED: Turned the sidebar app guide trigger into a real full-width button
  instead of letting it inherit the tiny delete-button styling.
- FIXED: Scoped the sidebar secondary-button CSS to horizontal remove rows only,
  so future sidebar buttons do not get crushed into icon-sized controls.
- CHANGED: Updated style regression coverage to verify both the primary guide
  button wiring and the tighter secondary-button CSS selector.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.11`.
- VALIDATED: `python -m unittest tests.test_styles -v`,
  `python -m unittest tests.test_sidebar -v`, and
  `python -m unittest discover -s tests -v` all passed locally.

v0.61.0 - FAQ button tone down
---------------------------------------------------------
- CHANGED: Renamed the sidebar guide trigger from `App guide` to `FAQ` so it is
  more instantly recognizable.
- FIXED: Switched the guide trigger from a bright primary button to a normal
  darker secondary button that fits the sidebar better.
- CHANGED: Updated the style regression check to expect the new `FAQ` label and
  secondary button type.
- CHANGED: Synced the visible footer version in `app.py` to `v0.58.12`.
- VALIDATED: `python -m unittest tests.test_styles -v`,
  `python -m unittest tests.test_sidebar -v`, and
  `python -m unittest discover -s tests -v` all passed locally.

v0.61.1 - compact in-chart axis label
---------------------------------------------------------
- CHANGED: Replaced the native x-axis and y-axis titles with a compact in-figure
  Plotly header such as `Points by Age in Regular szn` so the chart wastes less
  space.
- CHANGED: The in-figure header now switches between `Age`, `GP`, and `Season`
  plus `Regular szn`, `Play-offs`, or `Both` based on the active chart mode.
- CHANGED: Kept the new label inside the Plotly figure so the modebar camera
  export includes it in screenshots.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.1`.
- VALIDATED: `python -m unittest tests.test_chart -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 30/30
  tests.

v0.61.2 - share link title alignment fix
---------------------------------------------------------
- FIXED: Moved the in-figure chart header slightly lower so it sits more level
  with the Plotly modebar controls instead of crowding the top edge.
- FIXED: Repositioned the custom `Copy link` control to sit after the Plotly
  title text instead of overlapping underneath it.
- FIXED: Added a safety fallback so the share control drops below the title on
  tighter widths rather than crashing into the modebar buttons.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.2`.
- VALIDATED: `python -m unittest tests.test_chart -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 31/31
  tests.

v0.61.3 - semantic axis color cues
---------------------------------------------------------
- CHANGED: Replaced the single flat chart title with separate in-figure header
  annotations so the metric and x-context can be color-coded independently.
- CHANGED: Added soft blue highlighting for the y-side metric token and soft warm
  highlighting for the x-side context token, while keeping season scope neutral.
- CHANGED: Tinted the x-axis and y-axis tick labels to match those header cues so
  axis meaning reads faster at a glance.
- FIXED: Updated the custom `Copy link` positioning logic to anchor after the new
  header annotation instead of the old Plotly title node.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.3`.
- VALIDATED: `python -m unittest tests.test_chart -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 33/33
  tests.

v0.61.4 - tighter header spacing
---------------------------------------------------------
- FIXED: Tightened the header annotation spacing so words and chips no longer sit
  awkwardly far apart.
- CHANGED: Reduced the internal width estimate and padding used to position the
  semantic header segments while keeping the share-link anchor logic intact.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.4`.
- VALIDATED: `python -m unittest tests.test_chart -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 34/34
  tests.

v0.61.5 - normalized header spacing
---------------------------------------------------------
- FIXED: Rebalanced the chart header spacing so chip-to-word gaps look natural
  again instead of cramped.
- CHANGED: Increased the segment gap slightly and accounted for pill padding in
  the width estimate so text after highlighted chips no longer crowds them.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.5`.
- VALIDATED: `python -m unittest tests.test_chart -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 34/34
  tests.

v0.61.6 - reduced metric chip gap
---------------------------------------------------------
- FIXED: Reduced only the trailing space after the first highlighted metric chip
  so `Points` no longer sits awkwardly far from `by`.
- CHANGED: Added a dedicated smaller post-metric gap while leaving the rest of
  the header spacing unchanged.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.6`.
- VALIDATED: `python -m unittest tests.test_chart -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 34/34
  tests.

v0.61.7 - live games comparison tab
---------------------------------------------------------
- ADDED: A new `Live games` comparison tab that lists the next 4 upcoming NHL
  games with matchup, venue, and Central European local puck-drop time.
- ADDED: One-click `Add game` actions that add both teams plus each club's
  current-season points leader and best Save% goalie to comparison.
- CHANGED: The comparison panel now stays visible on an empty board so the Live
  games tab can seed players and teams without sidebar setup.
- ADDED: Regression coverage in `tests/test_schedule.py` and
  `tests/test_comparison.py` for upcoming-game parsing, time formatting,
  featured-player selection, and Live games tab wiring.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.7` and
  updated `README.md` plus `readme.txt` for the new feature.
- VALIDATED: `python -m unittest tests.test_schedule tests.test_comparison -v`
  passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with
  40/40 tests.

v0.61.8 - live games team logos
---------------------------------------------------------
- CHANGED: The `Live games` rows now show both teams' NHL logos beside the away
  and home team names instead of text-only matchup labels.
- ADDED: Focused regression coverage in `tests/test_comparison.py` to keep both
  logo URLs present in the matchup row markup.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.8`.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with
  41/41 tests.

v0.61.9 - live games button copy and logo sizing
---------------------------------------------------------
- CHANGED: Renamed the Live games action button from `Add game` to `Add to comparison`.
- CHANGED: Increased the matchup-row team logo height so both club icons read more clearly.
- ADDED: Regression checks in `tests/test_comparison.py` for the larger logo markup and updated button copy.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.9`.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 41/41 tests.

v0.61.10 - live games stacked button layout
---------------------------------------------------------
- CHANGED: Moved each Live games `Add to comparison` button underneath the matchup time and venue details instead of keeping it in a right-side column.
- ADDED: Regression coverage in `tests/test_comparison.py` to keep the button rendered after the date and venue caption.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.10`.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 41/41 tests.

v0.61.11 - live games short team names
---------------------------------------------------------
- CHANGED: Live games matchup rows now drop city names and show only team nicknames such as `Oilers`, `Bruins`, and `Red Wings`.
- ADDED: Regression checks in `tests/test_comparison.py` to keep both single-word and multi-word nicknames rendering correctly.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.11`.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 42/42 tests.

v0.61.12 - live games compact button spacing
---------------------------------------------------------
- CHANGED: Shrunk the Live games `Add to comparison` button with scoped CSS so it no longer spans the full row width.
- CHANGED: Tightened the space between each Live games button and the next matchup divider.
- ADDED: Regression checks in `tests/test_comparison.py` and `tests/test_styles.py` to keep the compact button layout scoped to Live games only.
- VALIDATED: `python -m unittest tests.test_comparison tests.test_styles -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 43/43 tests.

v0.61.13 - live games button spacing correction
---------------------------------------------------------
- FIXED: Replaced the failed Live games spacer-anchor CSS with a narrow button column so `Add to comparison` is actually smaller.
- FIXED: Tightened the caption-to-button and button-to-divider spacing using the real Live games content blocks instead of empty anchors.
- CHANGED: Kept the compact layout regression checks, but now assert the corrected column-based render path and scoped CSS selectors.
- VALIDATED: `python -m unittest tests.test_comparison tests.test_styles -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 43/43 tests.

v0.61.14 - live games compare label
---------------------------------------------------------
- CHANGED: Renamed the Live games action button from `Add to comparison` to `Compare` for a shorter, cleaner row layout.
- ADDED: Updated `tests/test_comparison.py` assertions to lock the new Live games button label in place.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 43/43 tests.

v0.61.15 - live games docs cleanup
---------------------------------------------------------
- CHANGED: Clarified `nhl/comparison.py` docstrings and inline comments around Live games tab selection and compact button layout.
- CHANGED: Confirmed the Live games tab is already documented in `README.md` and `readme.txt`, so no duplicate docs edits were needed.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 43/43 tests.

v0.61.16 - faq copy cleanup
---------------------------------------------------------
- FIXED: Removed the "hand over the sauce" caption from the sidebar FAQ dialog so the guide opens cleanly without that extra line.
- ADDED: A regression assertion in `tests/test_styles.py` to keep that phrase out of `nhl/dialog.py`.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.16`.
- VALIDATED: `python -m unittest tests.test_styles -v` passed locally.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 43/43 tests.

v0.61.17 - shared player landing cache
---------------------------------------------------------
- CHANGED: Added `get_player_landing()` as the shared cached NHL landing fetch so player metadata helpers reuse one payload instead of refetching the same endpoint.
- CHANGED: Refactored `get_player_headshot()`, `get_player_current_team()`, `get_player_roster_info()`, `get_player_hero_image()`, `get_player_awards()`, `get_player_league_abbrevs()`, and `get_player_raw_stats()` to derive from the shared landing payload while keeping silent fallbacks intact.
- CHANGED: Updated `readme.txt` and `CLAUDE.md` so `app.py` is documented as a ~350-line orchestrator and the caching docs match the new landing-helper flow.
- ADDED: Focused unittest coverage in `tests/test_data_loaders.py` for shared landing reuse, helper fallbacks, hero-image fallback behavior, inactive roster handling, and malformed awards payloads.
- VALIDATED: `python -m unittest tests.test_data_loaders -v` passed locally with 7/7 tests.

v0.61.18 - lazy comparison panel selector
---------------------------------------------------------
- CHANGED: Replaced eager `st.tabs()` comparison rendering with a selector-based `st.radio()` control so only the active Overview, Trophies, or Live games panel runs on each rerun.
- CHANGED: Preserved the existing per-category `panel_tab_*` state and URL param wiring, while making empty boards fall back to `Live games` only when `Overview` would be blank.
- CHANGED: Updated `readme.txt`, `README.md`, and the `app.py` module description so the docs now describe the selector-based comparison panel accurately.
- ADDED: Regression coverage in `tests/test_comparison.py` to verify that only the selected panel renders and that empty boards default to `Live games` without reviving hidden panel work.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.18`.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally with 6/6 tests.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 50/50 tests.

v0.61.19 - comparison tabs restoration
---------------------------------------------------------
- CHANGED: Reverted the comparison panel from selector-based `st.radio()` rendering back to native `st.tabs()` so Overview, Trophies, and Live games look and behave like the original UI again.
- CHANGED: Restored the native tab styling plus the tab-focused docs and comparison regression expectations.
- CHANGED: Synced the visible footer version in `app.py` to `v0.61.19`.
- VALIDATED: `python -m unittest tests.test_comparison -v` passed locally with 4/4 tests.
- VALIDATED: `python -m unittest discover -s tests -v` passed locally with 48/48 tests.

v0.61.20 - baseline cache boundary cleanup
---------------------------------------------------------
- CHANGED: Moved the cached historical and team baseline entry points to zero-arg wrappers so Streamlit no longer hashes large DataFrame arguments just to check cache hits.
- CHANGED: Kept `build_historical_baselines(df)` and `build_team_baselines(all_team_df)` as pure DataFrame builders, while `app.py` now calls `get_historical_baselines()` and `get_team_baselines()` for the cached path.
- CHANGED: Updated `readme.txt` and synced the visible footer version in `app.py` to `v0.61.20` so the cache strategy docs match runtime behavior.
- VALIDATED: `python -m unittest discover -s tests -p "test_baselines.py" -v` passed locally with 2/2 tests.
- VALIDATED: `python -m unittest discover -s tests -p "test_tail_shaping.py" -v` and `python -m unittest discover -s tests -v` passed locally with 4/4 and 50/50 tests.