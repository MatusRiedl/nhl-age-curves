=========================================================
NHL AGE CURVES - CHANGELOG
=========================================================

v0.62.16 - default dark theme
---------------------------------------------------------
- CHANGED: Added project-level Streamlit config at `.streamlit/config.toml` so the app now defaults to Streamlit's dark theme.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.16`.

v0.62.15 - lighter chart header copy
---------------------------------------------------------
- CHANGED: Made the chart description text above the plot regular weight instead of bold.
- CHANGED: Set that chart header text to white at `90%` opacity.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.15`.

v0.62.14 - softer axis tick labels
---------------------------------------------------------
- CHANGED: Set both x-axis and y-axis tick-label numbers to white at `80%` opacity instead of full white.
- CHANGED: Kept the in-chart axis cue labels (`Points`, `Age`, etc.) at `25%` white opacity.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.14`.

v0.62.13 - stronger in-chart cue labels
---------------------------------------------------------
- CHANGED: Bumped the in-chart axis cue labels (`Points`, `Age`, etc.) from `10%` to `25%` white opacity so they read a bit clearer without bringing back the tint.
- CHANGED: Kept both chart axes in straight white.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.13`.

v0.62.12 - chart label tint removal
---------------------------------------------------------
- CHANGED: Removed the red/blue tint from chart axis tick labels so both axes now render in straight white.
- CHANGED: Switched the in-chart axis cue labels (`Points`, `Age`, etc.) to the same white color at `10%` opacity.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.12`.

v0.62.11 - controls toolbar cleanup
---------------------------------------------------------
- CHANGED: Replaced the cramped 6-toggle control row with a compact pill-style `View options` toolbar using short labels.
- CHANGED: Preserved the existing session-state booleans so projection, smoothing, baseline, cumulative, era, and prime behavior still flows through the app unchanged.
- CHANGED: Removed the old toggle truncation CSS hacks, added muted unavailable option pills, and synced the visible footer version in `app.py` to `v0.62.11`.

v0.62.10 - softer support button with helper text
---------------------------------------------------------
- CHANGED: Shrunk the sidebar Ko-fi CTA so it feels less loud while keeping the same pill-style layout and coffee emoji.
- CHANGED: Added a second support line under `Buy me a coffee` that reads `Support the development`.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.10`.

v0.62.9 - ko-fi sidebar support button
---------------------------------------------------------
- CHANGED: Added a custom Ko-fi-style `Buy me a coffee` support button with a coffee emoji to the sidebar above `App status`.
- CHANGED: Added targeted sidebar/style tests to lock the Ko-fi CTA markup, placement, and CSS in place.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.9`.

v0.62.8 - search API failure fallback
---------------------------------------------------------
- CHANGED: Hardened `search_player()` against malformed NHL search payloads so a string or junk row no longer crashes the sidebar.
- CHANGED: Improved local player search to handle multi-token prefix queries like `Connor Mc` when the live search endpoint is down.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.8`.

v0.62.7 - more clone-driven ML blend
---------------------------------------------------------
- CHANGED: Switched the KNN clone-prior blend from `70/30` to `80/20` so projected targets follow age-specific clone evidence a bit more closely.
- CHANGED: Added a targeted invariant test that locks the new `80/20` blend math so future tweaks do not quietly drift it again.

v0.62.6 - softer dense late skater ML tail
---------------------------------------------------------
- CHANGED: Loosened the age-36+ skater KNN late-career guardrail so a small uptick can survive when at least four clones still provide real coverage.
- CHANGED: Kept the existing sparse fallback behavior for thinner late-age clone data so late skater rebounds still get shut down when support is weak.

v0.62.5 - comparison width and live button cleanup
---------------------------------------------------------
- CHANGED: Rebalanced the main chart/comparison split so the comparison panel gets a bit more horizontal room instead of feeling pinched.
- CHANGED: Removed the narrow spacer-column hack from the Live games `Compare` action so the button sizes to its label instead of clipping the text.
- CHANGED: Nudged the comparison tab strip upward to better align with the chart header row and `Copy link` control.

v0.62.4 - small-screen comparison button fix
---------------------------------------------------------
- CHANGED: Let the Live games comparison action row wrap on smaller screens so the `Compare` button can use the full available width instead of getting clipped.
- CHANGED: Kept the compact desktop layout for the comparison action row while allowing the mobile button label to break cleanly if space gets tight.

v0.62.3 - comparison row alignment polish
---------------------------------------------------------
- CHANGED: Pulled the main chart/comparison row closer to the `Category & Metric` expander so the page stops wasting vertical space above the visuals.
- CHANGED: Leveled the comparison tabs with the chart header row by matching the tab strip height and tightening its top spacing.
- CHANGED: Kept the laptop-first chart/comparison layout intact while trimming the leftover spacing slack around the row wrapper.

v0.62.2 - chart chrome cleanup for laptop screens
---------------------------------------------------------
- CHANGED: Replaced the in-plot header/overlay hack with a real toolbar row above the chart so the title and `Copy link` finally share one clean baseline.
- CHANGED: Shortened the chart height, tightened plot margins, and kept the Plotly modebar inside the chart's top-right corner where it belongs.
- CHANGED: Widened the chart vs. stats split and added an earlier laptop breakpoint so the right panel stacks below the chart before the layout gets cramped and ugly.

v0.63.0 - lean AI docs and docstring cleanup
---------------------------------------------------------
- CHANGED: Rewrote `CLAUDE.md` and `readme.txt` into shorter AI-facing handoff docs that keep the critical runtime rules without the token sludge.
- CHANGED: Trimmed verbose module and function docstrings across the core app, data, pipeline, UI, and schedule modules without removing the important behavior notes.
- CHANGED: Synced the visible footer version in `app.py` to `v0.62.1` and corrected the stale footer reference inside the `v0.62.0` changelog note.
- CHANGED: Forced dark theme and quality of life improvements.

v0.63.1 - grouped player and projection legend toggle
---------------------------------------------------------
- CHANGED: Linked each projected player trace to the same Plotly legend group as the real player line so one click now hides or shows both together.
- CHANGED: Removed the separate `(Proj)` legend item while keeping the dotted projected line and its glow styling on the chart.
- CHANGED: Added a targeted chart test that locks the grouped legend behavior in place.
- CHANGED: Synced the visible footer version in `app.py` to `v0.63.1`.

v0.63.2 - shorter baseline legend label
---------------------------------------------------------
- CHANGED: Renamed the player historical baseline legend item from the long skater/goalie percentile wording to `Reference baseline`.
- CHANGED: Added a targeted chart test so the shorter baseline label does not quietly drift back.
- CHANGED: Synced the visible footer version in `app.py` to `v0.63.2`.

v0.63.3 - dim baseline styling restored
---------------------------------------------------------
- CHANGED: Restored the player baseline line to the older dimmer styling so it reads as a greyed dashed reference line instead of a bright white overlay.
- CHANGED: Removed the extra baseline glow traces that were making the reference line look too hot on the dark chart.
- CHANGED: Updated the targeted chart source test and synced the visible footer version in `app.py` to `v0.63.3`.

v0.63.4 - baseline styling fix for renamed label
---------------------------------------------------------
- FIXED: Made baseline trace detection case-insensitive so the renamed `Reference baseline` legend item still renders as the muted dashed baseline instead of inheriting a normal player color.
- CHANGED: Added targeted chart tests that verify both baseline detection and the rendered baseline styling.
- CHANGED: Synced the visible footer version in `app.py` to `v0.63.4`.

v0.63.5 - baseline legend restyle
---------------------------------------------------------
- CHANGED: Restyled `Reference baseline` to a chunkier muted-grey dashed line with visible round markers so it matches the requested reference look.
- CHANGED: Moved the baseline style values into local chart constants and updated the targeted chart test to lock the new look in place.
- CHANGED: Synced the visible footer version in `app.py` to `v0.63.5`.

v0.63.6 - tighter mobile chart spacing
---------------------------------------------------------
- CHANGED: Reduced mobile page side padding so the main chart gets more of the screen instead of wasting space on the left gutter.
- CHANGED: Tightened the mobile chart toolbar and Plotly modebar spacing, and added responsive y-axis tick sizing so narrow screens stop feeling bloated.
- CHANGED: Updated the targeted chart/style tests and synced the visible footer version in `app.py` to `v0.63.6`.

v0.63.7 - mobile axis label alignment
---------------------------------------------------------
- CHANGED: Matched the mobile x-axis tick sizing to the responsive y-axis sizing so both axes stop fighting each other visually.
- CHANGED: Aligned the mobile chart title to Plotly's real left gutter so the header stops sitting left of the chart body.
- CHANGED: Updated the targeted chart tests and synced the visible footer version in `app.py` to `v0.63.7`.